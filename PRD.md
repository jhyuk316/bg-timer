# PRD: Board Game Timer

## 개요

보드게임 플레이 시 각 플레이어의 턴 시간을 관리하는 경량 웹앱.
GitHub Pages로 배포하며, 설치 없이 모바일/데스크톱 브라우저에서 바로 사용 가능.

## 기술 스택

| 항목 | 선택 | 이유 |
|------|------|------|
| 언어 | Vanilla JS (ES6+) | 의존성 제로, 빠른 로딩 |
| 스타일 | CSS (단일 파일) | 빌드 불필요 |
| 저장 | localStorage | 설정 영속화 |
| 멀티 동기화 | Firebase Realtime Database | 서버 없이 실시간 상태 공유 |
| 배포 | GitHub Pages | 무료, 간편 |
| PWA | Service Worker + manifest.json | 오프라인 사용, 홈화면 추가 |

## 핵심 요구사항

### 1. 플레이어 설정

설정 화면은 2페이지 위자드로 구성:
- **페이지1**: 10색 팔레트에서 미플 선택 (최대 5명) + 프리셋 버튼
- **페이지2**: 타이머 프리셋/세부값/옵션 설정

#### 미플 선택 (페이지1)
- 10색 팔레트 미플이 그리드로 표시 (dimmed/active 상태)
- 미플 탭 → 활성/비활성 토글 (최대 5명 제한)
- 활성 미플 아래 이름 입력 가능
- 프리셋 버튼 → 해당 게임의 팔레트 인덱스 미플만 자동 활성화
- 기본 활성: Crimson(인덱스0), Cobalt(인덱스6) — 2인
- 설정값은 localStorage에 저장하여 다음 접속 시 복원

#### 미플 색상 팔레트 (10색)

> 채도를 낮춘 뮤트 톤.

| # | 이름 | HEX | 인덱스 |
|---|------|---------|--------|
| 1 | Crimson | `#B84040` | 0 |
| 2 | Orange | `#CC7E3A` | 1 |
| 3 | Amber | `#C49645` | 2 |
| 4 | Green | `#4A9E6E` | 3 |
| 5 | Teal | `#4A9488` | 4 |
| 6 | Sky | `#6E9EB5` | 5 |
| 7 | Cobalt | `#3D6A9E` | 6 |
| 8 | Purple | `#7060A0` | 7 |
| 9 | Charcoal | `#455159` | 8 |
| 10 | Ivory | `#D8D4CF` | 9 |

#### 색상 프리셋 → 팔레트 매핑

프리셋 버튼을 누르면 해당 게임의 원본 색상에 대응하는 팔레트 미플만 활성화된다.

**SETI** — 우주 기관 테마. 4인용.

| 원본 | 팔레트 대체 | 인덱스 |
|------|------------|--------|
| Orange `#E08840` | Orange `#CC7E3A` | 1 |
| Green `#4CAF7A` | Green `#4A9E6E` | 3 |
| Purple `#7B5EA7` | Purple `#7060A0` | 7 |
| White `#E8E4DF` | Ivory `#D8D4CF` | 9 |

**백로성 (The White Castle)** — 일본 전통 톤. 4인용.

| 원본 | 팔레트 대체 | 인덱스 |
|------|------------|--------|
| Red `#C94040` | Crimson `#B84040` | 0 |
| Yellow `#D4B84E` | Amber `#C49645` | 2 |
| Green `#5A9E64` | Green `#4A9E6E` | 3 |
| Blue `#4477B0` | Cobalt `#3D6A9E` | 6 |

**버건디의 성 (The Castles of Burgundy)** — 유럽 중세 테마. 4인용.

| 원본 | 팔레트 대체 | 인덱스 |
|------|------------|--------|
| Red `#C94040` | Crimson `#B84040` | 0 |
| Green `#4A9E6E` | Green `#4A9E6E` | 3 |
| Blue `#3D6A9E` | Cobalt `#3D6A9E` | 6 |
| Dark Gray `#4A4545` | Charcoal `#455159` | 8 |

**엔데버 심해 (Endeavor Deep Sea)** — 심해 탐사 테마. 5인용.

| 원본 | 팔레트 대체 | 인덱스 |
|------|------------|--------|
| Red `#C94040` | Crimson `#B84040` | 0 |
| Yellow `#E0C850` | Amber `#C49645` | 2 |
| Mint `#50C4A8` | Teal `#4A9488` | 4 |
| Purple `#7B5EA7` | Purple `#7060A0` | 7 |
| Ivory `#E8E4DF` | Ivory `#D8D4CF` | 9 |

**윙스팬 (Wingspan)** — 조류 수집 테마. 5인용.

| 원본 | 팔레트 대체 | 인덱스 |
|------|------------|--------|
| Red `#C94040` | Crimson `#B84040` | 0 |
| Yellow `#D4B84E` | Amber `#C49645` | 2 |
| Green `#7EC060` | Green `#4A9E6E` | 3 |
| Blue `#5CADD4` | Cobalt `#3D6A9E` | 6 |
| Purple `#7B5EA7` | Purple `#7060A0` | 7 |

**Ian O'Toole** — 이안 오툴 아트워크 테마. 5인용.                                                                                                                                                    
                                                                                                                                                                                                     
| 원본 | 팔레트 대체 | 인덱스 |                                                                                                                                                                    
|------|------------|--------|
| Dusty Rose `#C4727A` | Crimson `#B84040` | 0 |
| Amber `#D4A24E` | Amber `#C49645` | 2 |
| Sage `#8BAF6E` | Green `#4A9E6E` | 3 |
| Powder Blue `#7CAFC4` | Sky `#6E9EB5` | 5 |
| Soft Purple `#8E7BB5` | Purple `#7060A0` | 7 |

### 2. 타이머 프리셋

단일 카운트다운 모드. 5개 프리셋 중 선택 후 세부값 수동 조정 가능.

| 프리셋 | 목표/인 | 메인 시간 | 턴 딜레이 | 패널티 추가시간 |
|--------|--------|----------|----------|---------------|
| Light      | 15분     | 10분    | 10초     | 1분            |
| Medium Light | 30분     | 20분    | 15초     | 2분            |
| Medium     | 1시간    | 50분    | 20초     | 3분            |
| Medium Heavy | 1.5시간  | 75분    | 30초     | 4분            |
| Heavy      | 2시간    | 100분   | 45초     | 5분            |

- **목표/인**: 1인당 예상 총 소요 시간 (약 30~40턴 기준)
- **메인 시간**: 플레이어별 메인 시간 (턴 간 유지, 내부 변수: mainTime)
- **턴 딜레이**: 매 턴 새로 부여되는 기본 시간 (내부 변수: turnTime)
- **패널티 추가시간**: 메인 시간 소진 시 패널티와 함께 부여되는 추가 시간

### 3. 타이머 동작

- 현재 턴 플레이어의 타이머만 진행 (동시에 두 개 이상의 타이머가 동작하지 않음)
- 일시정지 / 재개 기능
- 게임 리셋 기능
- 게임 종료 기능: 게임 종료 버튼 → 통계 화면으로 전환
- **백그라운드 동작**: 탭이 비활성화되어도 타이머 정확히 유지
  - `Date.now()` 기반 경과 시간 계산 (setInterval 드리프트 방지)
  - `visibilitychange` 이벤트로 복귀 시 시간 보정

#### 게임 상태 3종

- **플레이어 타이머 진행 중**: 특정 플레이어의 턴 타이머가 카운트다운 중
- **운영 타이머 진행 중**: 턴 사이 공백시간을 카운트업으로 측정 중
- **전체 정지**: 일시정지 상태 또는 게임 시작 전

#### 조작 규칙

| 현재 상태 | 동작 | 결과 |
|----------|------|------|
| 전체 정지 | 미플 A 탭 | A 타이머 시작 |
| A 진행 중 | 미플 A 탭 | A 정지 → 운영 타이머 시작 |
| A 진행 중 | 미플 B 탭 | A 정지 → B 즉시 시작 (직접 전환 숏컷) |
| 운영 진행 중 | 미플 B 탭 | 운영 정지 → B 타이머 시작 |
| 어떤 상태든 | 일시정지 버튼 | 전체 정지 |

- **게임 시작 상태**: 전체 정지. 아무 미플을 탭하면 해당 플레이어의 타이머가 시작된다.
- **직접 전환 숏컷**: A 진행 중 B를 탭하면 운영 타이머를 거치지 않고 B가 즉시 시작된다.

#### 운영 타이머

- 카운트업 방식 (턴 사이 공백시간 측정)
- 누적 운영 시간 기록
- 게임 종료 시 총 공백시간 확인 가능
- 상단 고정 바에 누적 운영 시간 표시 (비활성 시 최소화, 활성 시 확장)

#### 턴 흐름

1. 턴 시작 → 턴 딜레이 카운트다운
2. 턴 딜레이 소진 → 메인 시간 카운트다운 진입 (시각적 전환)
3. 메인 시간 소진 → **패널티 -2%** 적용 + 추가 메인 시간 부여
4. 반복마다 패널티 누적 (-2%, -4%, -6%, ...)

#### 턴 넘기기 시

- 남은 턴 딜레이: 소멸
- 메인 시간: 유지
- 다음 턴에 새 턴 딜레이 부여

#### 패널티

- 점수 기반: **-2%** (게임별 점수 폭이 다르므로 비율 적용)
- 소진 횟수에 비례하여 누적
- 화면에 패널티 횟수 및 누적 감점 표시

### 4. 알림

- 턴 딜레이 → 메인 시간 전환 시 경고 (색상 변화 + 선택적 소리)
- 메인 시간 잔여 5분 시점에 TTS 음성 알림 (Web Speech API, 예: "5분 남았습니다")
- 메인 시간 잔여 30% 시점에 추가 경고
- 메인 시간 소진 시 패널티 알림 (화면 플래시 + 소리)
- 패널티 누적 횟수 상시 표시
- 소리 on/off 토글

### 5. UI/UX

#### 테마
- 단일 테마: 뉴트럴 다크 톤 (파란기 없는 다크 그레이 배경, 밝은 텍스트)
- 배경: `#232628` ~ `#1f2224` 계열
- 서피스: `#2a2d32` ~ `#34383e`
- 강조: 플레이어 색상 활용
- 텍스트: `#dcdcdc` / dim: `#808890`

#### 화면 구성
- **설정 화면**: 2페이지 위자드 (페이지1: 미플 선택 + 프리셋, 페이지2: 타이머 설정)
- **게임 화면**:
  - 화면을 플레이어 수만큼 등분 (각 영역 = 미플 + 이름 + 타이머 + 패널티)
  - 활성 플레이어: 색상 강조 + 타이머 크게 표시
  - 운영 타이머: 상단 고정 바에 누적 운영 시간 표시 (비활성 시 최소화, 활성 시 확장)
  - 일시정지/리셋 버튼: 화면 가장자리 배치 (오조작 방지)
  - 레이아웃: 2인 상하 2등분 / 3인 상1+하2 / 4인 2×2 그리드 / 5인 상2+하3
- **통계 화면**: 게임 종료 후 결과 표시, 자동 저장된 기록의 게임명 수정, 새 게임 버튼
- **히스토리 화면**: 저장된 게임 목록, 선택 시 통계 상세 열람
- 화면 전환은 JS로 DOM 토글 (SPA 방식, 라우터 불필요)

#### 반응형
- 모바일 우선 설계 (보드게임은 테이블 위 스마트폰 사용이 주 시나리오)
- 데스크톱에서도 정상 동작

### 6. PWA
- `manifest.json`: 앱 이름, 아이콘, 테마 색상
- Service Worker: 오프라인 캐싱 (Cache First 전략)
- 홈 화면 추가 가능

### 7. 멀티플레이

여러 기기가 하나의 타이머 방에 접속하여 현재 턴, 남은 시간, 버튼 상태를 실시간으로 공유하는 모드.
GitHub Pages 정적 프론트엔드와 Firebase Realtime Database만 사용하며, 별도 백엔드 서버는 두지 않는다.

#### 7-1. 지원 환경

- iOS Safari
- Android Chrome
- 데스크톱 브라우저
- 서로 다른 Wi-Fi/LTE 네트워크
- GitHub Pages 배포 환경

#### 7-2. 핵심 시나리오

1. 방장이 멀티 방을 만든다.
2. 앱이 방 코드와 QR 코드를 보여준다.
3. 게스트는 QR 코드 또는 방 코드로 접속한다.
4. 접속자는 사용할 플레이어 말을 하나 이상 고른다.
5. 한 기기가 여러 말을 맡을 수 있다.
6. 방장 기기의 선택 말 수와 게스트 기기의 선택 말 수를 합산하여 실제 참가자 수로 본다.
7. 모든 말 선택이 끝나면 방장이 게임을 시작한다.
8. 게임 중 모든 기기는 현재 턴, 남은 시간, 일시정지/재개, 게임 종료 상태를 같은 값으로 본다.
9. 접속이 끊긴 기기는 같은 방 코드로 재접속할 수 있다.

예시:
- 방장 폰이 3개 말을 선택
- 게스트 폰이 1개 말을 선택
- 총 4명의 플레이어가 게임에 참여

#### 7-3. 역할

| 역할 | 권한 |
|------|------|
| 방장 | 방 생성, 설정 변경, 플레이어 말 확정, 게임 시작, 일시정지/재개, 게임 종료 |
| 게스트 | 방 참가, 담당 플레이어 말 선택/해제, 자신이 맡은 말의 턴 버튼 조작 |
| 관전자 | 현재 상태 보기만 가능. MVP에서는 선택 기능 없음 |

MVP에서는 방장과 게스트만 구현한다. 관전자는 추후 확장으로 둔다.

#### 7-4. 방 생성

- 방 생성 시 Firebase에 새 room 레코드를 만든다.
- 방 ID는 URL에 포함되는 고유 ID로 사용한다.
- 사용자가 직접 입력하는 방 코드는 짧은 대문자/숫자 조합으로 제공한다.
- 방 화면에는 다음을 표시한다.
  - QR 코드
  - 방 코드
  - 공유 URL
  - 현재 접속자 목록
  - 선택된 플레이어 말 현황

#### 7-5. 방 참가

- QR 코드 스캔 시 `?room={roomId}` 또는 `#/room/{roomId}` 형태로 앱에 진입한다.
- 방 코드 입력 시 Firebase에서 해당 방을 조회한다.
- 접속자는 표시 이름을 입력하거나 기본 이름을 사용한다.
- 같은 브라우저에서 재접속하면 localStorage에 저장된 `clientId`로 기존 접속자로 복원한다.

#### 7-6. 플레이어 말 선택

- 기존 10색 팔레트와 프리셋 구조를 그대로 사용한다.
- 멀티 방에서는 각 말에 소유 기기(`ownerClientId`)가 붙는다.
- 한 기기는 여러 말을 선택할 수 있다.
- 이미 다른 기기가 선택한 말은 잠금 상태로 표시한다.
- 방장은 필요 시 모든 말 선택을 해제하거나 특정 말의 소유자를 변경할 수 있다.
- 게임 시작 후에는 말 선택을 잠근다.

#### 7-7. 타이머 동기화 원칙

타이머 값을 매초 Firebase에 쓰지 않는다.
Firebase에는 턴 변경, 시작, 일시정지, 재개, 종료 같은 이벤트와 기준 시각만 저장한다.
각 기기는 Firebase에서 받은 기준 상태와 자신의 `Date.now()`를 사용해 화면의 남은 시간을 계산한다.

저장 이벤트:
- 게임 시작
- 턴 시작
- 운영 타이머 시작
- 직접 턴 전환
- 일시정지
- 재개
- 게임 종료
- 플레이어별 시간/패널티 정산

저장하지 않는 값:
- 매초 줄어드는 남은 시간
- 화면 표시용 tick
- 애니메이션 상태

#### 7-8. 시간 계산 모델

Firebase에 저장하는 기준 상태:

| 필드 | 설명 |
|------|------|
| `status` | `lobby`, `running`, `paused`, `ended` |
| `mode` | `player`, `admin`, `idle` |
| `activePlayerId` | 현재 턴 플레이어. 운영 타이머 중이면 `null` |
| `turnStartedAt` | 현재 턴/운영 타이머가 시작된 서버 기준 시각 |
| `pausedAt` | 일시정지 시작 시각 |
| `players.{id}.remainingMainMs` | 마지막 정산 기준 메인 잔여 시간 |
| `players.{id}.turnDelayMs` | 매 턴 기본 딜레이 |
| `players.{id}.penaltyCount` | 패널티 횟수 |
| `admin.elapsedMs` | 마지막 정산 기준 운영 타이머 누적 시간 |
| `revision` | 동시 업데이트 충돌 방지용 증가 값 |

남은 시간 계산:
- 현재 기기 시간과 `turnStartedAt`의 차이로 경과 시간을 계산한다.
- 턴 딜레이가 남아 있으면 먼저 차감한다.
- 턴 딜레이가 0이 되면 메인 시간을 차감한다.
- 메인 시간이 0 이하가 되면 패널티 규칙을 적용하고 추가 시간을 부여한다.
- 일시정지 중에는 `pausedAt` 기준으로 화면을 고정한다.

서버 기준 시각:
- 이벤트 기록에는 Firebase 서버 타임스탬프를 사용한다.
- 클라이언트는 접속 시 서버 오프셋을 읽어 로컬 시간과 서버 시간 차이를 보정한다.

#### 7-9. Firebase 데이터 구조 초안

```json
{
  "rooms": {
    "{roomId}": {
      "code": "A7K2",
      "createdAt": 1720000000000,
      "updatedAt": 1720000000000,
      "hostClientId": "client_host",
      "status": "lobby",
      "settings": {
        "presetId": "medium",
        "mainTimeMs": 3000000,
        "turnDelayMs": 20000,
        "penaltyExtraMs": 180000,
        "soundEnabled": true
      },
      "clients": {
        "client_host": {
          "name": "Host",
          "role": "host",
          "connected": true,
          "lastSeenAt": 1720000000000
        }
      },
      "players": {
        "p0": {
          "name": "Crimson",
          "color": "#B84040",
          "paletteIndex": 0,
          "ownerClientId": "client_host",
          "remainingMainMs": 3000000,
          "turnDelayMs": 20000,
          "penaltyCount": 0,
          "totalActiveMs": 0,
          "turnCount": 0
        }
      },
      "timer": {
        "mode": "idle",
        "activePlayerId": null,
        "turnStartedAt": null,
        "pausedAt": null,
        "adminElapsedMs": 0,
        "revision": 0
      },
      "events": {
        "{eventId}": {
          "type": "turnStart",
          "clientId": "client_host",
          "playerId": "p0",
          "createdAt": 1720000000000,
          "revision": 1
        }
      }
    }
  },
  "roomCodes": {
    "A7K2": "{roomId}"
  }
}
```

#### 7-10. 동시 조작과 충돌 처리

- 모든 턴 변경은 Firebase transaction 또는 revision 비교로 처리한다.
- 같은 순간 두 기기가 버튼을 눌렀을 경우 먼저 반영된 이벤트만 유효하다.
- 늦게 도착한 클라이언트는 최신 room 상태를 다시 받아 화면을 갱신한다.
- 버튼을 누른 직후에는 로컬 낙관 업데이트를 할 수 있지만, Firebase 결과와 다르면 서버 상태를 우선한다.

#### 7-11. 접속 상태와 재접속

- 클라이언트마다 localStorage에 `clientId`를 저장한다.
- Firebase presence 또는 `lastSeenAt`으로 접속 상태를 표시한다.
- 연결이 끊겨도 방과 타이머는 계속 유지된다.
- 재접속 시 기존 `clientId`가 있으면 맡은 플레이어 말을 복원한다.
- 다른 기기에서 같은 이름으로 접속해도 같은 사용자로 자동 병합하지 않는다.

#### 7-12. 보안 규칙 방향

- 읽기/쓰기는 방 ID를 아는 사용자에게 허용하는 단순 모델로 시작한다.
- 방장 전용 필드는 `hostClientId`와 요청 `clientId`가 일치할 때만 변경 가능하도록 규칙을 둔다.
- MVP에서는 로그인 기능을 넣지 않는다.
- 방 코드는 공유 편의용이며 보안 토큰으로 간주하지 않는다.
- 오래된 방은 일정 기간 후 삭제할 수 있도록 `updatedAt`을 기록한다.

#### 7-13. QR 코드

- QR 코드는 방 URL을 담는다.
- 외부 빌드 없이 동작해야 하므로 작은 QR 생성 라이브러리를 정적 파일로 포함한다.
- QR API는 사용하지 않는다. 오프라인/PWA와 개인정보 측면에서 부적합하다.
- QR 아래에는 방 코드를 항상 함께 표시한다.

#### 7-14. 실패/예외 상태

- 방을 찾을 수 없음: 방 코드 재입력 화면 표시
- 네트워크 끊김: 상단에 연결 끊김 상태 표시, 조작 버튼 비활성화
- 권한 없음: 방장 전용 조작임을 표시
- 플레이어 말 충돌: 최신 선택 상태로 갱신하고 다시 선택하도록 안내
- Firebase 설정 없음: 멀티 기능 비활성화, 싱글 모드는 정상 사용

#### 7-15. MVP 범위

포함:
- 방 생성
- QR/방 코드 참가
- 접속자 이름 입력
- 여러 플레이어 말 선택
- 현재 턴/남은 시간/일시정지/재개/종료 동기화
- 재접속 복원
- 싱글 모드 유지

제외:
- 로그인
- 친구 목록
- 채팅
- 관전자 모드
- 방 목록 공개 탐색
- 서버 백엔드
- WebRTC/블루투스/Wi-Fi Direct

#### 7-16. 완료 기준

- 방장 폰 1대와 게스트 폰 1대가 서로 다른 네트워크에서 같은 방에 접속할 수 있다.
- 방장 폰이 3개 말, 게스트 폰이 1개 말을 선택해 총 4명 게임을 시작할 수 있다.
- 어느 기기에서든 턴 버튼을 누르면 모든 기기에서 현재 턴이 바뀐다.
- 일시정지/재개 상태가 모든 기기에 반영된다.
- 화면을 껐다 켜거나 브라우저를 새로고침해도 같은 방으로 복귀할 수 있다.
- Firebase에는 매초 쓰기가 발생하지 않는다.

### 8. 게임 통계 & 히스토리

#### 8-1. 통계 화면

게임 종료 시 표시되는 결과 화면.

**표시 항목:**

| 항목 | 설명 |
|------|------|
| 전체 플레이 타임 | 게임 시작~종료 총 경과 시간 |
| 플레이어별 총 소요 시간 | 각 플레이어 타이머 누적 합계 |
| 플레이어별 턴 수 | 각 플레이어가 활성화된 턴 횟수 |
| 플레이어별 패널티 | 패널티 횟수 및 누적 감점 |
| 운영 총 시간 | 운영 타이머 누적 합계 (공백시간) |
| 운영 턴 수 | 운영 타이머가 활성화된 횟수 |

**화면 구성:**
- 상단: 전체 플레이 타임
- 중앙: 플레이어별 통계 카드 (미플 색상 적용)
- 하단: 운영 통계 + 게임명 수정/새 게임 버튼

**동작:**
- 게임 종료 버튼 → 확인 다이얼로그 → 히스토리에 자동 저장 → 통계 화면 전환
- 통계 화면에서 게임명 입력 후 "이름 저장 & 새 게임" → 저장된 히스토리 기록명 수정
- 게임명 입력 시 기존에 저장된 게임명 자동완성 제안 (같은 보드게임을 반복 플레이할 때 편의)
- "새 게임" 버튼 → 설정 화면으로 이동

#### 8-2. 히스토리

**저장 데이터 구조 (localStorage):**
- 게임명 (사용자 입력, 기본값: "Game - {날짜}")
- 날짜/시간
- 플레이어 목록 (이름, 색상)
- 타이머 프리셋/설정값
- 플레이어별 통계 (총 시간, 턴 수, 패널티)
- 운영 통계 (총 시간, 턴 수)
- 전체 플레이 타임

**히스토리 화면:**
- 설정 화면에서 접근 (히스토리 버튼)
- 저장된 게임 목록 (게임명, 날짜, 플레이어 수)
- 게임 선택 → 통계 상세 열람
- 삭제 기능

#### 8-3. Firebase 플레이 기록

멀티플레이 도입 후에는 완료된 게임 기록을 Firebase에도 저장할 수 있다.
단, 실시간 방 상태와 완료된 기록은 목적이 다르므로 데이터 경로를 분리한다.

| 경로 | 목적 | 보존 기간 |
|------|------|----------|
| `rooms/{roomId}` | 진행 중인 방 상태, 턴 동기화, 접속 상태 | 짧게 유지 후 정리 |
| `gameRecords/{recordId}` | 종료된 게임의 플레이 기록과 통계 | 사용자가 삭제할 때까지 유지 가능 |

**저장 대상:**
- 게임명
- 저장 시각
- 싱글/멀티 여부
- 멀티 방 ID
- 플레이어 목록 (이름, 색상, 담당 clientId)
- 타이머 프리셋/설정값
- 전체 플레이 타임
- 플레이어별 총 소요 시간
- 플레이어별 턴 수
- 플레이어별 패널티 횟수/감점
- 운영 타이머 총 시간/횟수
- 저장한 clientId

**저장 시점:**
- 게임 종료 확인 직후 기록한다.
- 통계 화면에서는 이미 저장된 기록의 게임명을 수정할 수 있다.
- 멀티 게임은 Firebase `gameRecords` 저장을 우선 지원한다.
- 싱글 게임은 기존 localStorage 저장을 유지하고, 추후 "클라우드 저장" 옵션으로 확장한다.

**조회 범위:**
- MVP에서는 방 코드 또는 같은 브라우저의 `clientId` 기준으로 기록을 조회한다.
- 로그인 없이 시작하므로 여러 기기 간 "내 기록" 통합은 지원하지 않는다.
- 추후 Firebase Anonymous Auth를 도입하면 사용자별 기록 묶기와 기기 변경 복원이 가능하다.

**Firebase 데이터 구조 초안:**

```json
{
  "gameRecords": {
    "{recordId}": {
      "gameName": "SETI",
      "mode": "multi",
      "roomId": "{roomId}",
      "createdAt": 1720000000000,
      "savedByClientId": "client_host",
      "totalElapsedMs": 7200000,
      "settings": {
        "presetId": "medium",
        "mainTimeMs": 3000000,
        "turnDelayMs": 20000,
        "penaltyExtraMs": 180000
      },
      "players": {
        "p0": {
          "name": "Crimson",
          "color": "#B84040",
          "ownerClientId": "client_host",
          "totalActiveMs": 1800000,
          "turnCount": 14,
          "penaltyCount": 0,
          "penaltyScore": 0
        }
      },
      "admin": {
        "elapsedMs": 420000,
        "turnCount": 8
      }
    }
  }
}
```

**단계별 범위:**

1. localStorage 히스토리 유지
2. 멀티 게임 종료 기록을 Firebase `gameRecords`에 저장
3. 기록 화면에서 Firebase 기록 조회 추가
4. 싱글 게임도 선택적으로 Firebase에 저장
5. 익명 로그인 또는 계정 연동으로 사용자별 기록 통합

## 파일 구조

```
bg-timer/
├── index.html
├── css/
│   └── style.css
├── js/
│   ├── app.js          # 앱 초기화, 화면 전환
│   ├── timer.js        # 타이머 로직 (코어)
│   ├── settings.js     # 설정 관리, localStorage
│   ├── ui.js           # DOM 조작, 렌더링
│   ├── sound.js        # 알림 사운드 (Web Audio API)
│   ├── history.js      # 히스토리 저장/조회, localStorage/Firebase 기록
│   ├── firebase.js     # Firebase 초기화, 서버 시간 보정
│   ├── room.js         # 방 생성/참가/동기화 상태 관리
│   └── qrcode.js       # 정적 QR 코드 생성 라이브러리 또는 래퍼
├── manifest.json
├── sw.js               # Service Worker
├── icons/              # PWA 아이콘
└── PRD.md
```

## 설계 원칙

1. **빌드 없음**: HTML/CSS/JS 파일을 그대로 서빙
2. **싱글 모드 우선**: Firebase 설정이 없어도 기존 싱글 타이머는 정상 동작
3. **모듈 분리**: 기능별 JS 파일 분리 (ES Modules)
4. **과하지 않게**: 필요한 기능만, 최소한의 추상화
5. **쓰기 최소화**: 멀티 모드는 매초 저장하지 않고 이벤트 단위로만 동기화

## 참고: 보드게임 타이머 방식

| 방식 | 설명 | 대표 사례 |
|------|------|----------|
| Fischer increment | 턴마다 고정 시간 추가, 총 시간 내에서 운용 | 체스 |
| Byoyomi | 기본 시간 소진 후 짧은 추가시간을 무한 반복 | 바둑 |
| Scrabble 토너먼트 | 총 시간 초과 시 분당 점수 감점 | 스크래블 |
| BGA (Board Game Arena) | 플레이어별 총 시간 제한, 초과 시 강제 턴 넘김 | BGA 온라인 |

**본 프로젝트 타이머 = Fischer(턴 시간) + Scrabble(점수 패널티) + Byoyomi(무한 추가시간) 하이브리드**

- Fischer: 매 턴 새 턴 딜레이 부여
- Scrabble: 총 시간 초과 시 점수 비율(-2%) 패널티
- Byoyomi: 패널티 추가시간으로 게임이 강제 종료되지 않음

### 조작 방식 비교

| 조작 방식 | 설명 | 대표 사례 |
|----------|------|----------|
| 순차 전환 | 고정 순서 "다음" 버튼 | 체스 클럭, Board Game Timer |
| 자유 선택 | 아무 플레이어 탭 | Multi Player Chess Clock |
| 운영 타이머 | 턴 사이 공백시간 별도 추적 | Shared Game Timer (Admin Timer) |

**본 프로젝트 조작 방식 = 자유 선택 + 운영 타이머 + 직접 전환 숏컷**

- 자유 선택: 턴 순서가 유동적인 보드게임 특성에 맞춰 아무 플레이어나 탭으로 전환
- 운영 타이머: 턴 사이 토론·규칙 확인 등 공백시간을 별도 카운트업으로 추적
- 직접 전환 숏컷: A 진행 중 B 탭 시 운영 단계 없이 즉시 B 시작 (빠른 턴 전환)

## 향후 확장 가능 (현재 스코프 아님)

- 턴 순서 랜덤화
- 커스텀 효과음
- 다국어 지원
- 관전자 모드
- 방장 이전
- Firebase 오래된 방 자동 정리
